import { StyleSheet, TouchableOpacity } from 'react-native';

import { Text, View } from './Themed';

export default function History({ path }: { path: string }) {
  return (
    <View>
        <Text>History</Text>
    </View>
  );
}

const styles = StyleSheet.create({
    
});
